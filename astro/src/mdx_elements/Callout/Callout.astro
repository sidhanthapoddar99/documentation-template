---
/**
 * Callout - Info/warning/danger/success/tip callouts for MDX
 *
 * Usage:
 * <Callout type="info" title="Note">
 *   This is an info callout.
 * </Callout>
 */

export interface Props {
  type?: 'info' | 'warning' | 'danger' | 'success' | 'tip' | 'note';
  title?: string;
  class?: string;
}

const { type = 'info', title, class: className = '' } = Astro.props;

const icons: Record<string, string> = {
  info: 'ℹ️',
  warning: '⚠️',
  danger: '❌',
  success: '✅',
  tip: '💡',
  note: '📝',
};

const defaultTitles: Record<string, string> = {
  info: 'Info',
  warning: 'Warning',
  danger: 'Danger',
  success: 'Success',
  tip: 'Tip',
  note: 'Note',
};
---

<aside class:list={['callout', `callout--${type}`, className]} role="note">
  <div class="callout__header">
    <span class="callout__icon" aria-hidden="true">{icons[type]}</span>
    <span class="callout__title">{title || defaultTitles[type]}</span>
  </div>
  <div class="callout__content">
    <slot />
  </div>
</aside>

<style>
  .callout {
    margin: 1.5rem 0;
    padding: 1rem 1.25rem;
    border-radius: 0.5rem;
    border-left: 4px solid;
  }

  .callout__header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .callout__icon {
    font-size: 1.125rem;
    line-height: 1;
  }

  .callout__title {
    font-weight: 600;
    font-size: 0.875rem;
  }

  .callout__content {
    font-size: 0.875rem;
    line-height: 1.625;
  }

  .callout__content :global(p:first-child) {
    margin-top: 0;
  }

  .callout__content :global(p:last-child) {
    margin-bottom: 0;
  }

  /* Info */
  .callout--info {
    background-color: var(--callout-info-bg);
    border-left-color: var(--callout-info-border);
    color: var(--callout-info-text);
  }

  /* Warning */
  .callout--warning {
    background-color: var(--callout-warning-bg);
    border-left-color: var(--callout-warning-border);
    color: var(--callout-warning-text);
  }

  /* Danger */
  .callout--danger {
    background-color: var(--callout-danger-bg);
    border-left-color: var(--callout-danger-border);
    color: var(--callout-danger-text);
  }

  /* Success */
  .callout--success {
    background-color: var(--callout-success-bg);
    border-left-color: var(--callout-success-border);
    color: var(--callout-success-text);
  }

  /* Tip */
  .callout--tip {
    background-color: var(--callout-tip-bg);
    border-left-color: var(--callout-tip-border);
    color: var(--callout-tip-text);
  }

  /* Note */
  .callout--note {
    background-color: var(--theme-bg-secondary);
    border-left-color: var(--theme-border-strong);
    color: var(--theme-text-primary);
  }

  @media (max-width: 640px) {
    .callout {
      padding: 0.875rem 1rem;
    }
  }
</style>
