## Goal

A first-class `issues` content type — folder-per-issue data model, vocabulary-driven metadata, index + detail pages, subtask interactivity, multi-file support (issue.md + comments + subtasks + notes + agent-log).

The tracker you're reading this in is the implementation.

See subtasks for the workstreams.
