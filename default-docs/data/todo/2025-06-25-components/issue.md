## Goal

A coherent component story for the framework — the markdown-rendered components (code blocks, diagrams).

See subtasks for the remaining workstreams.

> **Scope change (2026-04-20):** the "UI components library" subtask (originally `subtasks/03_components-library.md`) has been moved to `2026-04-20-custom-tags/subtasks/01_components-library.md`. That work now lives under the custom-tags issue because the UI components in question (cards = callout, tabs, accordion = collapsible) are implemented as custom-tag transformers and share the same pipeline-wiring dependency. Badges and buttons travelled with them but may later be reclassified as theme-layer utility classes.
