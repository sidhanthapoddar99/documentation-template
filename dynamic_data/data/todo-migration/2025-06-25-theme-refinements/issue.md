## Goal

Theme switching is smooth with no flash; every variable in the contract is documented.

## Tasks

- [x] Complete CSS variable coverage for layout dimensions (`--max-width-primary` / `--max-width-secondary`)
- [x] Update all layouts to use new variables (docs, blog, navbar, footer, features)
- [x] Test theme switching (light / dark)
- [x] Fix Shiki syntax highlighting in both themes
- [x] Improve dark mode transitions (smooth fade)
- [x] Document all theme variables
