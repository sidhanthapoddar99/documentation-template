## Goal

Diagrams render live inside the editor pane — same Mermaid / Graphviz pipeline that already serves the rendered docs, plus an inline Excalidraw surface for `.excalidraw` files.

See subtasks for the workstreams.
