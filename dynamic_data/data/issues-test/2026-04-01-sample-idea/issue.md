## Sketch

A second view on `/issues` that groups cards by `milestone`, with columns per `status`. Users can drag cards between columns to update status (writes back to settings.json).

Orthogonal to the filter-chips view — both can coexist via a view toggle.
