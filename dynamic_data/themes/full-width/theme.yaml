name: "Full Width"
version: "1.0.0"
description: "Full-width layout — content stretches to fill the viewport with sidebar pinned left"
extends: "@theme/default"
supports_dark_mode: true

files:
  - element.css
