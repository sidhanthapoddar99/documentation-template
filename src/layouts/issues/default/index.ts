/**
 * Default issues layout
 *
 * Components:
 * - IndexLayout/IndexBody: filterable list view at /<base_url>
 * - DetailLayout/DetailBody: per-issue view at /<base_url>/<id>
 * - IssueCard: list row
 * - MetaPanel: editable sidebar for settings.json
 * - StatusBadge: colored pill for status / priority / type
 */

export const config = {
  name: 'default',
  description: 'Filterable list + per-issue detail view',
};
