---
/**
 * Blog Style 1 - Index Layout
 * Wrapper that imports the component with styles
 */
import IndexBody from './IndexBody.astro';

interface Props {
  dataPath: string;
  postsPerPage?: number;
}

const props = Astro.props;
---

<IndexBody {...props} />
