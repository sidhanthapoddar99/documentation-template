---
/**
 * Blog Style 1 - Post Layout
 * Wrapper that imports the component with styles
 */
import PostBody from './PostBody.astro';

interface Props {
  title: string;
  description?: string;
  date?: string;
  author?: string;
  tags?: string[];
  content: string;
}

const props = Astro.props;
---

<PostBody {...props} />
